<?php
session_start();
include 'db.php';
use PHPMailer\PHPMailer\PHPMailer;
use PHPMailer\PHPMailer\Exception;

require 'vendor/autoload.php';

// เช็คว่าเป็นการเข้าใช้งานจาก role 'teacher' หรือไม่
if ($_SESSION['role'] !== 'teacher') {
    sendAccessDeniedEmail();
    die("รายละเอียดได้ถูกส่งไปยังผู้ดูแลระบบ");
}

// ฟังก์ชันในการส่งอีเมล์แจ้งเตือน
function sendAccessDeniedEmail() {
    $mail = new PHPMailer(true);
    
    try {
        $mail->isSMTP();
        $mail->Host = 'smtp.gmail.com';
        $mail->SMTPAuth = true;
        $mail->Username = 'sittikorn-sat@rmutp.ac.th';
        $mail->Password = 'zufl kvea nykx fylf'; // ใช้ App Password ถ้ามี
        $mail->SMTPSecure = PHPMailer::ENCRYPTION_STARTTLS;
        $mail->Port = 587;

        // ใช้ setFrom() และส่งอีเมลหาตัวเอง
        $mail->setFrom('sittikorn-sat@rmutp.ac.th', 'ระบบการนัดหมาย');
        $mail->addAddress('sittikorn.nice@gmail.com'); // ส่งหาตัวเอง

        $mail->isHTML(true);
        $mail->Subject = 'การเข้าถึงผิดพลาดของระบบ';
        $mail->Body    = "มีนักศึกษาได้จองนัดหมาย กรุณาเข้าสู่ระบบเพื่อเช็คและยืนยันสถานะการนัดหมายของนักศึกษา";

        $mail->send();
    } catch (Exception $e) {
        echo "Message could not be sent. Mailer Error: {$mail->ErrorInfo}";
    }
}


// ดึงข้อมูลนัดหมายสำหรับอาจารย์
$teacher_id = $_SESSION['user_id'];
$appointments = $conn->query("SELECT * FROM appointments WHERE teacher_id = $teacher_id");

?>

<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>การนัดหมายของอาจารย์</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div class="container">
        <h2>รายการการนัดหมาย</h2>
        <?php while ($appointment = $appointments->fetch_assoc()) { ?>
            <p>นัดหมาย: <?php echo $appointment['appointment_date'] . ' ' . $appointment['appointment_time']; ?> 
                <a href="approve.php?id=<?php echo $appointment['id']; ?>">ยืนยัน</a> 
                <a href="reject.php?id=<?php echo $appointment['id']; ?>">ปฏิเสธ</a></p>
        <?php } ?>
    </div>
</body>
</html>
